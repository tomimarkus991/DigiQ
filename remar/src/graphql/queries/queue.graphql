query GetQueue($id: Int!) {
  queue(id: $id) {
    id
    name
    estimatedServingtime
    shortestWaitingTime
    longestWaitingTime
    imageUri
    waiting
    creator {
      username
    }
  }
}
